Artifact: V120DocumentationCompatibilityAudit
Batch: v1.2.0 Batch 5

BRANCH
- branch name: feature/v1.2.0-documentation-compatibility
- starting commit: c99edf7 (Batch 4 ending commit)

IMPLEMENTATION FACTS EXTRACTED
- package name: @dailephd/my-dev-kit-orchestrator
- package version: 1.1.0 (unchanged in this batch; v1.2.0 is implemented on branches but not yet published, tagged, or released)
- CLI bin: my-dev-kit-orchestrator (node dist/cli.js)
- supported npm scripts: build, build:watch, start, docs:check, smoke:cli, test, test:security, test:watch, lint, lint:docs, typecheck, clean
- modes (source: src/types.ts VALID_MODES): feature, repair, test, refactor, harden, extraction, greenfield (7 modes)
- greenfield stages (source: src/greenfield/modes/greenfieldStages.ts GREENFIELD_STAGE_NAMES): idea-brief, product-boundary, stack-decision, starter-profile, bootstrap-bundle, project-docs, scaffold-plan, scaffold-implementation, first-vertical-slice, verification, initial-index, judge, final-report (13 stages)
- greenfield artifact paths (source: src/greenfield/modes/greenfieldMode.ts): 10 stage-specific paths + 3 shared paths (verification-report.txt, judge-report.txt, final-report.txt) = 13 total
- greenfield profiles (source: src/greenfield/profiles/resolveGreenfieldProfile.ts SUPPORTED_PROFILES): typescript-cli, nextjs-app, android-compose (3 profiles)
- Android Compose profile facts (source: androidComposeProfile.ts): stack = Kotlin, Jetpack Compose, Gradle, Android app module; setupCommands = []; validationCommands = ./gradlew build (required), ./gradlew testDebugUnitTest (required), ./gradlew connectedAndroidTest (optional, device/emulator-dependent); orchestrator never executes any of these
- resolver facts (source: resolveGreenfieldProfile.ts): bounded alias table (android, kotlin-compose, jetpack-compose, compose-android), no fuzzy matching; generic "mobile"/"mobile app"/"phone app" -> 'unresolved'; iOS/Flutter/React Native unaliased -> 'unsupported'; platformTarget-only Android auto-selection not implemented (open question, unchanged)
- bootstrap/scaffold facts (source: buildBootstrapBundle.ts, validateBootstrapDocs.ts, buildScaffoldPlan.ts): buildGreenfieldBootstrapBundle is generic; validation rules and validateBootstrapDocs are profile-conditional (Android/Jetpack allowed only for android-compose; unsupported-platform and Play-Store/release claims always rejected); buildScaffoldPlan reads setupCommands/validationCommands directly from the profile; entry-point detection includes .kt
- CLI/check/export facts (source: src/commands/start.ts, prompt.ts, export.ts, src/artifactChecker.ts/promptChecker.ts/contractChecker.ts, confirmed by grep): start never parses the request into a profile; prompt is static mode+stage-keyed text; check pipeline has zero Android-specific logic; export is generic (stage/artifact checklist plus one verification-report.txt passthrough channel); safe/unsafe export path behavior unchanged
- known limitations (from Batches 1-4's own reports, re-confirmed, not re-derived): no Gradle execution, no Android SDK requirement, no Play Store readiness claim, no Flutter/React Native/iOS support, no generic mobile mode, platformTarget-only Android auto-selection unresolved, optional Kotlin-indexing check status (see below)

DOCUMENTS INSPECTED
- README.md -- status: stale (2 hits: stale two-profile claim, stale "no Android/mobile" limitation)
- CHANGELOG.md -- status: correct as-is (v1.1.0 entry is accurate history), missing a v1.2.0/Unreleased entry
- docs/ROADMAP.md -- status: stale (planned-vs-actual mismatch: CLI --profile flag and auto-routing were planned but not built; "will add" future-tense wording now stale)
- docs/USAGE.md -- status: stale (2 hits: stale "does not provide an Android or mobile profile" claim, stale/undersold export-traversal-boundary disclaimer contradicting the CHANGELOG's own "fixed" claim)
- docs/WORKFLOWS.md -- status: stale (known two-profile sentence, "v1.1.0 supports...not Android" framing, and version-pinned "seven workflow modes in v1.1.0" wording)
- docs/ARTIFACTS.md -- status: stale (validateBootstrapDocs description said it unconditionally rejects Android/mobile, which is now false; missing setupCommands/validationCommands GreenfieldProfileCommand[] documentation)
- docs/DEVELOPMENT.md -- status: correct as far as it went, but missing required "Adding a greenfield profile" guidance (section 16 requirement)
- docs/ARCHITECTURE.md -- status: correct as far as it went, but missing a profile-driven-architecture section (section 17 requirement); "Non-goals" and "Check and export boundaries" sections were already accurate and needed no change
- docs/RELEASE_CHECKLIST.md -- status: stale (1 hit: "Android/mobile support remains deferred beyond v1.1.0"), plus missing required v1.2.0/Android Compose smoke checklist items

STALE CLAIMS FOUND
- document: README.md
  stale claim: "The `v1.1.0` foundation is platform-neutral. It supports the `typescript-cli` and `nextjs-app` starter profiles. It does not implement Android or other mobile profiles."
  source-of-truth correction: 3 profiles are supported (typescript-cli, nextjs-app, android-compose); Android Compose is profile-guided planning, not a build runner
  fix required: yes (fixed)

- document: README.md
  stale claim: "no Android/mobile support in `v1.1.0`" (Current limitations)
  source-of-truth correction: Android Compose IS supported; the real limitation is scope (no Gradle execution, no SDK requirement, generic mobile unresolved, iOS/Flutter/React Native unsupported)
  fix required: yes (fixed)

- document: docs/WORKFLOWS.md
  stale claim: "The v1.1.0 foundation is platform-neutral. It supports the `typescript-cli` and `nextjs-app` profiles, not Android or other mobile profiles." (the exact sentence flagged by Batch 1's audit and hardcoded into the pre-Batch-5 docs:check script)
  source-of-truth correction: same as above, plus clarification that CLI `start`/`prompt` never resolve a profile at all (true for every profile, not new to Android Compose)
  fix required: yes (fixed)

- document: docs/WORKFLOWS.md
  stale claim: "`my-dev-kit-orchestrator` supports seven workflow modes in v1.1.0."
  source-of-truth correction: mode count/list is version-independent; still 7 modes, unaffected by v1.2.0
  fix required: yes, minor (fixed -- removed the version pin, since the mode list itself was already correct)

- document: docs/USAGE.md
  stale claim: "The v1.1.0 greenfield foundation is platform-neutral and does not provide an Android or mobile profile."
  source-of-truth correction: same profile-list correction as above; added an explicit Android Compose usage example per section 13.2
  fix required: yes (fixed)

- document: docs/USAGE.md
  stale claim: "Do not rely on `--out` as a complete path-traversal boundary; normalized parent paths are a known cross-mode follow-up."
  source-of-truth correction: CHANGELOG.md's own v1.1.0 entry says this traversal guard was fixed ("fixed the cross-mode export --out traversal guard so raw parent-path traversal attempts...are rejected"), and Batch 4's CLI smoke run empirically confirmed `--out ../unsafe-export.txt` is correctly rejected with no file written outside the target directory. This USAGE.md sentence contradicted the project's own shipped, tested behavior.
  fix required: yes (fixed) -- not an Android-related finding, but caught during the required stale-claim sweep and squarely within "make every user-facing document compatible with the actual implementation"

- document: docs/ARTIFACTS.md
  stale claim: "`validateBootstrapDocs` checks required content and rejects unsupported Android/mobile and release/security/publishing claims." (stated as an unconditional rejection)
  source-of-truth correction: Batch 3 made this profile-conditional; Android/Jetpack content is now permitted specifically for android-compose
  fix required: yes (fixed), plus added GreenfieldProfileCommand[]/setupCommands/validationCommands documentation per section 15.1

- document: docs/RELEASE_CHECKLIST.md
  stale claim: "Android/mobile support remains deferred beyond `v1.1.0`" (Scope and limitations audit)
  source-of-truth correction: same profile-list correction; also added the required v1.2.0 Android Compose smoke checklist section (18.2) and updated the "Current release baseline" bullets to target the next release version generically rather than hardcoding 1.1.0
  fix required: yes (fixed)

- document: docs/ROADMAP.md
  stale claim: the "Planned milestones > v1.2.0" entry described a `--profile` CLI flag and automatic signal-based routing to android-compose ("a request like 'start a new Android app' should route to greenfield mode with `android-compose` profile when Android or Compose signals are present"), neither of which was built; it also used future tense ("will add") for a version that is now implemented
  source-of-truth correction: no CLI --profile flag exists; no auto-routing was implemented (deliberate scope decision, not an oversight -- see Batch 2's "open design question"); Gradle command specifics and scaffold-plan file targets also differ from what was originally sketched
  fix required: yes (fixed) -- converted to an "Implemented v1.2.0" section (mirroring the existing "Implemented v1.1.0" pattern) with an explicit "Deviations from the original plan" subsection, rather than silently rewriting the original plan to match reality

- document: scripts/check-docs-consistency.mjs
  stale claim (in logic, not prose): hardcoded regex `/supports the \`typescript-cli\` and \`nextjs-app\` profiles/i` and hardcoded misleading-claim patterns `/supports android/i`, `/supports mobile/i` (the latter two would false-positive on a legitimate "supports Android Compose" claim)
  source-of-truth correction: profile list must be extracted from SUPPORTED_PROFILES in resolveGreenfieldProfile.ts, the same way modes/stages are already extracted from source
  fix required: yes (fixed) -- see reports/v1.2.0-docs-check-report.txt for full detail

CORRECT HISTORICAL CLAIMS (left unchanged)
- document: CHANGELOG.md
  claim: "v1.1.0...deferred Android and mobile profiles to v1.2.0 or later" / "Android/mobile support remains deferred to v1.2.0 or later" (Known limitations under the v1.1.0 entry)
  why it remains: this was true at the time v1.1.0 was released; CHANGELOG entries describe what was true when that version shipped, not current state (section 9.1)

- document: docs/ROADMAP.md
  claim: "no Android/mobile profile in v1.1.0" and "v1.1.0 does not include Android/mobile behavior or Android static analysis." (under "Implemented v1.1.0")
  why it remains: correct, version-scoped historical statements about what v1.1.0 specifically shipped with; the "Implemented v1.2.0" section added by this batch supersedes them for current state without needing to edit the v1.1.0 section

CORRECT ROADMAP/FUTURE CLAIMS (left unchanged)
- document: docs/ROADMAP.md
  claim: v1.3.0 "Mobile Profile Expansion and Scaffold Verification" (possible additional profiles: android-xml, python-cli, stronger typescript-cli/nextjs-app), v1.4.0 handoff hardening, v1.5.0 "Optional Mobile Profile Candidates" (ios-swiftui, flutter, react-native)
  why it remains: these are genuinely future, unimplemented plans; nothing in Batches 1-5 implements or contradicts them. Not reorganized, not moved between versions, per section 12.1.

AMBIGUOUS CLAIMS
- none required a stop-and-ask user decision. The one genuinely ambiguous item -- how to describe ROADMAP's planned-vs-actual v1.2.0 divergence -- was resolved using information already established and reported across Batches 1-4 (the explicit "open design question" framing from Batch 2 onward), not a new decision requiring user input. See "Stale claims found > docs/ROADMAP.md" above for the resolution taken.

KNOWN BATCH 4 STALE-DOC TARGETS
- README.md stale two-profile/no-Android claim status: confirmed present, fixed in this batch
- docs/WORKFLOWS.md stale two-profile/no-Android claim status: confirmed present (the exact sentence Batch 1 originally flagged), fixed in this batch
- scripts/check-docs-consistency.mjs hardcoded profile regex status: confirmed present (both the two-profile sentence regex and the `/supports android/i`/`/supports mobile/i` false-positive-prone patterns), refactored in this batch to be source-derived

STATUS: complete
