Artifact: V120Batch4TestReport
Batch: v1.2.0 Batch 4

TEST FILES ADDED
- tests/greenfield/androidComposeCliRegression.spec.ts -- purpose: CLI-level (in-process, via createProgram().parse) coverage proving Android Compose requests work through init/start/prompt/status/list, and that the previously-stale starter-profile/scaffold-plan/scaffold-implementation/project-docs stage prompts now correctly describe android-compose as supported (13 tests)
- tests/greenfield/androidComposeCheckExport.spec.ts -- purpose: check/export regression coverage proving the CLI check pipeline accepts legitimate Android/Kotlin/Gradle fixture content, fresh-run missing-artifact behavior is unchanged, export carries Android Compose request/evidence content correctly, and export path safety (safe + unsafe traversal) is unaffected by profile (13 tests)

TEST FILES CHANGED
- none. No pre-existing test asserted the exact stale prompt wording this batch corrected (grep-confirmed before editing), so no existing test needed updating -- unlike Batches 2 and 3, this batch introduced zero regressions against the pre-existing suite.

ANDROID COMPOSE CLI COVERAGE
- covered: init (workspace creation); start --mode greenfield with an explicit Android Compose request succeeds and stays mode: greenfield (never "mobile" or "android-compose" as a mode); prompt/status/list all succeed for the run; the starter-profile stage prompt correctly lists android-compose as a v1.2.0-supported profile and no longer blanket-forbids Android/mobile profiles; the scaffold-plan stage prompt gives Gradle/Android-neutral guidance (asks for "setup commands"/"validation commands" generically, never hardcodes npm install/npm run build) and no longer blanket-forbids Android/mobile file trees; the scaffold-implementation stage prompt no longer blanket-forbids Android/mobile behavior; the project-docs stage prompt correctly states Android/mobile claims are allowed conditionally (only when the selected profile is android-compose), not forbidden unconditionally; across all 8 relevant stages, no prompt output claims Gradle ran, the Android SDK is available, or Play Store readiness; TypeScript CLI and Next.js greenfield requests still start and prompt successfully (regression, via `it.each`).
- uncovered: whether a coding agent, given these corrected prompts, would actually produce a correct android-compose starter-profile.json/scaffold-plan.txt artifact end-to-end -- this batch tests the *prompt text itself* is accurate, not an LLM's response to it, since this project's CLI does not call an LLM directly (per agents.txt rule 9) and no such agent-in-the-loop harness exists in this repository's test suite.
  reason for uncovered case: out of scope by design -- the orchestrator's job (per agents.txt) is to generate correct, bounded prompts and let a coding agent execute them; testing agent *compliance* with a prompt is not something this test suite does for any stage or any mode, not just Android Compose, so adding it here would be inventing a new testing paradigm beyond this batch's (and the project's) scope.

ANDROID COMPOSE CHECK COVERAGE
- covered: check --artifacts resolves 13 stage contracts for an Android-Compose-requested fresh run (same as any profile, since contracts are stage-name-keyed not profile-keyed); CLI check --artifacts exits 1 for a fresh run (0/13 present) exactly as expected/documented; check accepts a present idea-brief.json fixture containing "android-compose"/"Kotlin"/"Jetpack Compose"/"Gradle" text without any false failure; check accepts a present project-docs-report.txt fixture mentioning AndroidManifest/MainActivity/Gradle without any false failure; the contract summary itself still contains no react-native/flutter artifact requirement (regression, unchanged from v1.1.0); check --all runs to completion (all 4 sections) for a fresh Android Compose run without throwing.
- uncovered: whether check --all's stage-gate logic behaves any differently for an android-compose run with a judge-report.txt present but no verification-report.txt (Gate 5 violation) -- this exact scenario is already covered generically (profile-independent) in tests/greenfield/greenfieldCheckExport.spec.ts's "stage gates: judge-requires-verification..." test, which passed unchanged in this batch's full-suite run, so a duplicate Android-Compose-flavored copy was judged unnecessary (stage gates operate on artifact presence/mode, never on profile or artifact content).
  reason for uncovered case: would be pure duplication of already-passing, profile-independent coverage; per this project's own principle of parsimony, not added.

ANDROID COMPOSE EXPORT COVERAGE
- covered: export includes greenfield mode and all 13 stage-checklist lines for an Android Compose run (same mechanism as any profile); export includes the literal Android Compose request text verbatim (the "Request" section is the one place export carries per-run content); export correctly carries a Gradle-flavored verification-report.txt fixture's evidence text verbatim (via readVerificationEvidence) without ever asserting the build succeeded beyond what the fixture itself states; a fresh export makes no Play-Store/release-ready/Android-SDK-availability claim anywhere; the CLI export command works end-to-end and prints the mode + request to stdout.
- uncovered: whether export would surface bootstrap-bundle.json/starter-profile.json/project-docs-report.txt content directly (e.g. "this run's profile is android-compose") if those artifacts were present.
  reason for uncovered case: architecturally impossible to test as "covered," because `buildExportText` (src/commands/export.ts) does not read those files' content at all, for any profile -- only verification-report.txt content is echoed. This is documented as an intentional architectural finding (see the CLI regression report's "Check/export behavior" section), not a gap Batch 4 introduced or should fill; adding such a feature would be a scope-expanding production change beyond "prefer tests only... modify production code only if tests reveal user-facing behavior cannot correctly represent Android Compose" (section 7.6) -- export already represents Android Compose exactly as well as it represents every other profile, which is the correct bar.

EXPORT PATH SAFETY COVERAGE
- safe path: covered, both via jest (directory-entry-count assertion: exactly one new file appears in the owned temp dir) and the real built-CLI smoke run (section 21.4).
- unsafe traversal path: covered, both via jest (`--out ../mdko-unsafe-android-export.txt` rejected with exit 1 and a "path traversal" message, with a defensive existence check + cleanup in case it were ever written) and the real built-CLI smoke run.
- no outside write: covered in both forms; the jest test asserts `fs.existsSync(resolvedUnsafe)` is false, and the real CLI smoke run's `ls` of the parent directory confirmed no unsafe file was created.

EXISTING PROFILE/MODE REGRESSION COVERAGE
- TypeScript CLI: covered via `it.each` in the new CLI regression file, plus the full pre-existing tests/greenfield/greenfieldCliRegression.spec.ts suite passing unchanged.
- Next.js: covered the same way.
- existing greenfield: all pre-existing tests/greenfield/*.spec.ts files pass unchanged (16 suites, 248 tests in the `tests/greenfield` run).
- other modes through full suite: `npm test` (44 suites, 1123 tests) confirms feature/repair/test/refactor/harden/extraction and every non-greenfield test file passes unchanged; this batch touched no files outside src/promptGenerator.ts (greenfield section only), src/greenfield/scaffold/renderScaffoldPrompt.ts, and src/greenfield/bootstrap/populateProjectDocsFromBrief.ts (a comment), so no other mode's behavior could have been affected.

NO ANDROID TOOLING EXECUTION
- confirm tests do not execute Gradle: confirmed, by source inspection of both new test files (no child_process/execSync/spawnSync usage) and by the fact the full suite passes with no Gradle installed in this environment.
- confirm tests do not require Android SDK: confirmed, same evidence.
- confirm tests do not run emulator/device checks: confirmed; "connectedAndroidTest" appears only as plain string/fixture-text data in both new test files, never invoked.

TEMP DIRECTORY SAFETY
- dedicated temp dirs: both new test files use `fs.mkdtempSync(path.join(os.tmpdir(), 'mdko-android-cli-'))` / `'mdko-android-check-export-'`, following the exact naming-prefixed-uniqueness convention already used by every other greenfield test file in this project.
- cleanup behavior: every test wraps its body in try/finally with `fs.rmSync(dir, { recursive: true, force: true })`, matching the existing convention; the unsafe-export-path test additionally has a defensive extra cleanup for the (expected-never-created) file outside the temp dir.
- no shared os.tmpdir listing-count assumptions: confirmed; the one test that counts directory entries (`export path safety - Android Compose run > safe export path...`) counts entries only within its own freshly-created, exclusively-owned temp directory (`tmp`), never `os.tmpdir()` itself -- following the same pattern already used in tests/greenfield/androidComposeBootstrapBundle.spec.ts and androidComposeScaffoldPlan.spec.ts from Batch 3.

TRACEABILITY

- requirement: start/prompt/status/list succeed for an explicit Android Compose greenfield request; run stays mode: greenfield (section 11.1, 11.3)
  test file: tests/greenfield/androidComposeCliRegression.spec.ts
  test case: "start --mode greenfield succeeds for an explicit Android Compose request", "does not create a mobile/android-compose mode...", "prompt succeeds...", "status succeeds...", "list succeeds..."

- requirement: prompt output surfaces Android Compose profile guidance where stage-appropriate, without npm/Gradle-execution/SDK/Play-Store claims (section 11.2, 7.2)
  test file: tests/greenfield/androidComposeCliRegression.spec.ts
  test case: "starter-profile stage prompt correctly names android-compose...", "scaffold-plan stage prompt gives Gradle/Android-neutral guidance...", "scaffold-implementation stage prompt does not blanket-forbid...", "project-docs stage prompt allows Android/mobile claims conditionally...", "no prompt output claims Gradle was run..."

- requirement: check --artifacts/--all work for a fresh Android Compose run with expected nonzero exit; legitimate Android terms are accepted (section 11.4, 7.3)
  test file: tests/greenfield/androidComposeCheckExport.spec.ts
  test case: "resolves 13 stage contracts...", "CLI check --artifacts fails with the expected nonzero exit...", "accepts a present idea-brief artifact containing Android/Kotlin/Compose/Gradle terms", "accepts a present project-docs-report artifact...", "runs all v1 checks for a fresh Android Compose run without throwing"

- requirement: export includes Android Compose context, does not claim Gradle/SDK/Play-Store facts, safe/unsafe path behavior unchanged (section 11.5, 7.4)
  test file: tests/greenfield/androidComposeCheckExport.spec.ts
  test case: "includes greenfield mode and all 13 stage artifact expectations...", "includes the Android Compose request text verbatim...", "carries verification-report.txt Gradle evidence verbatim...", "does not claim Play Store readiness...", "safe export path...succeeds...", "unsafe export path (.. traversal) is rejected..."

- requirement: existing TypeScript CLI, Next.js, greenfield, and non-greenfield mode behavior does not regress (section 7.5)
  test file: tests/greenfield/androidComposeCliRegression.spec.ts (new `it.each` regression describe block) plus the full pre-existing suite
  test case: "%s still starts and prompts successfully" (TypeScript CLI, Next.js); full `npm test` run (1123 tests)

STATUS: complete
