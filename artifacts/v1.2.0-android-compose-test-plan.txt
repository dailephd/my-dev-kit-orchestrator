Artifact: V120AndroidComposeTestPlan
Batch: v1.2.0 Batch 1

PURPOSE
Define the tests required for v1.2.0 implementation batches 2 through 5, traceable to the architecture decisions in artifacts/v1.2.0-android-compose-profile-contract.txt.

BATCH 2 TESTS (Android Compose profile foundation)

File: tests/greenfield/profileResolution.spec.ts (extend the existing Batch 2-from-v1.1.0 file; do not fork a new file for this, since it already owns profile-resolution coverage)

- Android Compose profile tests:
  - explicit `preferredProfile: 'android-compose'` resolves to `status: 'selected'`, `profile.id === 'android-compose'`
  - resolved profile includes `setupCommands: []` and `validationCommands: ['./gradlew build', './gradlew testDebugUnitTest']`
  - resolved profile's stackAssumptions include Kotlin, Jetpack Compose, Gradle
- profile resolution tests (aliases):
  - `preferredProfile: 'android'` resolves via alias to android-compose, `reason` mentions the alias
  - `preferredProfile: 'Android App'` (case/spacing variant) resolves via alias to android-compose
  - `preferredProfile: 'kotlin-compose'` and `'jetpack-compose'` both resolve via alias to android-compose
- brief normalization tests: none needed (confirmed in the architecture audit that brief schema/normalization requires no changes; `preferredProfile`/`platformTarget` already pass through verbatim). If Batch 2 discovers this assumption was wrong, it must return to Batch 1's architecture decision, not invent a workaround.
- unsupported profile tests:
  - `preferredProfile: 'react-native'`, `'flutter'`, `'ios'` all still resolve to `status: 'unsupported'` (regression: these must NOT become aliased to android-compose or to each other)
  - a brief that signals generic "mobile" (e.g. `platformTarget: 'mobile'`, no `preferredProfile`) resolves to `status: 'unresolved'` (new behavior), not `'selected'` and not `'unsupported'`
  - the `'unresolved'` result's `reason` mentions that android-compose is available and must be explicitly requested
- TypeScript CLI regression: existing explicit/fallback typescript-cli tests continue to pass unchanged; additionally assert `profile.setupCommands`/`profile.validationCommands` now exist and equal today's implicit values (`['npm install']` / `['npm run typecheck', 'npm run build', 'npm test']`)
- Next.js regression: same as above for nextjs-app

BATCH 3 TESTS (Android Compose bootstrap bundle, docs, and scaffold planning)

File: tests/greenfield/bootstrapBundle.spec.ts (extend existing file)
- Android Compose bootstrap bundle tests:
  - bundle built from an android-compose profile selection has `stackDecision.chosenStack` containing Kotlin/Jetpack Compose/Gradle
  - `validationRules` contains `no-unsupported-mobile-platform-claims` and `android-claims-require-android-compose-profile` (not the old `no-android-mobile-claims`)
  - `scaffoldPlanningInputs.profileId === 'android-compose'`
- deterministic output tests: same bundle built twice from the same normalized brief + profile selection produces identical output (existing determinism test pattern, extended to an android-compose fixture)
- no disk-write tests: existing "performs no filesystem writes" test pattern, extended to an android-compose fixture (should pass trivially, since the bundle builder has no profile-specific I/O)
- no Gradle execution tests: assert that building a bundle/scaffold plan for android-compose never shells out to `gradle`/`./gradlew` (e.g. by asserting no `child_process` usage in the relevant modules, or by running the test in an environment without Gradle on PATH and confirming no error)
- no Android SDK requirement tests: assert the same functions succeed in this environment (which has no Android SDK installed) -- their success here is itself the regression test.

File: tests/greenfield/bootstrapProjectDocs.spec.ts (extend existing file)
- Android Compose project docs tests:
  - docs generated from an android-compose bundle mention Kotlin/Jetpack Compose/Gradle appropriately (e.g. in stack-decision, starter-profile-summary, development-workflow docs)
  - `validateBootstrapDocs(result, 'android-compose')` returns `valid: true` for genuinely android-compose-flavored content
  - `validateBootstrapDocs(result, 'typescript-cli')` (or with profile id omitted) still flags an Android/Jetpack mention as `android-mobile-claim` -- i.e. the conditional permission is profile-specific, not global
  - `validateBootstrapDocs()` still flags iOS/React Native/Flutter/Kotlin-Multiplatform/Compose-Multiplatform mentions as forbidden regardless of profile (regression, always-on check)
  - component docs remain empty for android-compose (regression: unchanged reasoning from v1.1.0)

File: tests/greenfield/scaffoldPrompt.spec.ts (extend existing file)
- Android Compose scaffold plan tests:
  - `buildScaffoldPlan()` for an android-compose bundle produces `setupCommands: []`, `validationCommands: ['./gradlew build', './gradlew testDebugUnitTest']`
  - planned file groups include a "configuration" group (settings.gradle.kts, build.gradle.kts) and a "source" group (app/... paths)
  - `firstRunnableBehavior.entryPoint` detects a `.kt` file when one is present in `templateTargets` (regression for the broadened entry-point regex; also assert `.ts`/`.tsx` detection still works for the other two profiles)
  - no Android/mobile term appears in a typescript-cli or nextjs-app scaffold plan (regression, unchanged)
  - scaffold plan generation performs no filesystem writes, installs nothing, runs no commands (existing pattern, extended)

BATCH 4 TESTS (Android Compose CLI/check/export regression coverage)

File: tests/greenfield/greenfieldCliRegression.spec.ts (extend existing file)
- Android Compose CLI regression:
  - `start --mode greenfield "<idea>"` with an android-compose-oriented brief/idea succeeds identically to the existing typescript-cli/nextjs-app cases (same stage order, same artifact paths -- profile does not change either)
  - prompt/status/list all behave identically regardless of which profile is ultimately selected (they operate on stage/mode, not profile)
- check --artifacts: unchanged behavior expected (artifact paths are stage-keyed, not profile-keyed); one regression test using an android-compose-flavored run to confirm no profile-specific branching leaked into contract checking
- check --all: same as above
- export: same as above; existing export.ts has zero profile awareness, so this is confirmation, not new coverage
- export path safety regression: no new tests required (architecture audit confirms export.ts never reaches profile-specific code); existing traversal-rejection tests already cover every mode including greenfield, and profile selection happens entirely before export.ts is ever invoked
- existing profile regression: full re-run of typescript-cli and nextjs-app through the CLI dispatcher (already covered by existing tests; confirm they still pass unchanged after Batch 2/3's additive profile-field changes)
- existing mode regression: feature/repair/test/refactor/harden/extraction unaffected (existing pattern, re-run as part of `npm test`)

BATCH 5 TESTS (documentation compatibility)

File: src/__tests__/docs-consistency.test.ts (extend existing file, the owner for docs:check coverage) and/or direct invocation of `npm run docs:check`
- docs:check tests:
  - `runDocsConsistencyCheck()` passes after Batch 5's doc updates
  - the refactored profile-list check (extracting `SUPPORTED_PROFILES` keys or `GreenfieldProfileId` union members from source, per the architecture audit's recommendation) correctly requires all three profiles (`typescript-cli`, `nextjs-app`, `android-compose`) to be mentioned in docs/WORKFLOWS.md, and fails if any one is missing (regression-test the check itself, not just its passing state)
  - the narrowed `misleadingAndroidClaims` patterns no longer flag "supports Android Compose" as a violation
  - the narrowed patterns still flag "supports react native", "supports flutter", "supports ios" as violations (regression)
  - new stale-claim patterns ("Android Compose planned", "planned Android Compose", "v1.2.0 planned") are detected if present (mirroring the existing v1.1.0-planned check)
- documentation stale-claim checks: covered by the above.
- README Android Compose current-support checks: docs:check's existing "docs do not present X as current" style assertions, extended to require android-compose is presented as current (not planned) once Batch 5 ships.
- roadmap preservation checks: not applicable as an automated test (ROADMAP.md content is not machine-checked beyond the existing "does not say v1.1.0 is awaiting pre-release"-style stale-claim regexes); Batch 5 should add an analogous "does not say v1.2.0 is awaiting pre-release" check if it follows the same historical-exception pattern as v1.1.0's own docs hardening.
- artifact docs consistency checks: existing `extractGreenfieldArtifactPaths()`-driven check requires no changes (Android Compose introduces zero new artifact paths, confirmed in the architecture audit), but should be re-run as regression to confirm this remains true after Batch 2-4's changes.

TEST CONVENTIONS
- Use tests/greenfield/*.spec.ts for all Batch 2-4 additions (extending the existing owner files named above), consistent with the v1.1.0 precedent and section 6.7's explicit preference.
- Update src/__tests__/docs-consistency.test.ts (its established owner) for Batch 5's docs:check changes, not a new file under tests/greenfield/, since docs-consistency.test.ts already exists as the correct owner (mirrors the "if existing test files are the correct owner, update them narrowly instead" principle already applied in the v1.1.0 release-risk repair for run-export.test.ts).
- Do not consolidate the test-directory split (src/__tests__/*.test.ts vs tests/**/*.spec.ts) in v1.2.0 unless a later batch is explicitly asked to do so; this plan deliberately follows the existing split rather than resolving it.

STATUS: complete
