Artifact: V120BatchSequencePlan
Batch: v1.2.0 Batch 1

PURPOSE
Define the later v1.2.0 implementation batches based on the architecture audit and profile contract, so each batch has a bounded, unambiguous scope and required inputs.

BATCH 2: Android Compose profile foundation

- goal: register `android-compose` as a fully resolvable greenfield profile, with the two new profile fields (`setupCommands`, `validationCommands`) added to the profile contract and backfilled onto the existing two profiles.
- files likely to change:
  - src/greenfield/profiles/profileTypes.ts (add `'android-compose'` to `GreenfieldProfileId`; add `setupCommands: string[]` and `validationCommands: string[]` to `GreenfieldProfile`)
  - src/greenfield/profiles/androidComposeProfile.ts (new file, mirroring typescriptCliProfile.ts's shape)
  - src/greenfield/profiles/typescriptCliProfile.ts, nextjsAppProfile.ts (add the two new required fields, values matching current implicit behavior)
  - src/greenfield/profiles/resolveGreenfieldProfile.ts (register android-compose in `SUPPORTED_PROFILES`; add `PROFILE_ALIASES`; add the new `MOBILE_HINT_RE`/"unresolved" fallback branch)
- tests to add/update: tests/greenfield/profileResolution.spec.ts (see artifacts/v1.2.0-android-compose-test-plan.txt, "Batch 2 tests" section, in full)
- validation commands: `npx jest tests/greenfield/profileResolution.spec.ts --silent`, `npx tsc --noEmit`, `npm test`, `npm run build`
- stop conditions: do not touch bootstrap bundle, docs bootstrap, or scaffold planning files yet (those are Batch 3); do not touch prompt generation yet (Batch 2 may update greenfieldStarterProfilePrompt's hardcoded profile-list prose if it is quick and low-risk, but the primary prompt-wording pass belongs to whichever batch needs it -- decide at Batch 2 time based on how large that change actually is); do not add CI/docs changes.
- required input artifacts: artifacts/v1.2.0-android-compose-profile-contract.txt, artifacts/v1.2.0-android-compose-do-not-expand-list.txt, artifacts/v1.2.0-android-compose-test-plan.txt (Batch 2 section), reports/v1.2.0-android-compose-architecture-audit.txt.

BATCH 3: Android Compose bootstrap bundle, docs, and scaffold planning

- goal: make GreenfieldBootstrapBundle, docs bootstrap, and scaffold planning fully Android-Compose-aware, using Batch 2's profile as input.
- files likely to change:
  - src/greenfield/bootstrap/buildBootstrapBundle.ts (`buildValidationRules()` split into `no-unsupported-mobile-platform-claims` + `android-claims-require-android-compose-profile`; `buildUnresolvedDecisions()` gains an `'unresolved'`-status branch)
  - src/greenfield/bootstrap/validateBootstrapDocs.ts (new profile-id parameter; `ANDROID_MOBILE_RE` split into always-on vs profile-conditional checks)
  - any call sites of `validateBootstrapDocs()` (trace and update to pass the profile id)
  - src/greenfield/scaffold/buildScaffoldPlan.ts (`setupCommands`/`validationCommands` sourced from `profile.setupCommands`/`profile.validationCommands` instead of hardcoded npm logic; entry-point regex broadened to `.kt`)
  - src/greenfield/scaffold/renderScaffoldPrompt.ts (spot-check only; likely no wording changes needed, per architecture audit)
- tests to add/update: tests/greenfield/bootstrapBundle.spec.ts, bootstrapProjectDocs.spec.ts, scaffoldPrompt.spec.ts (see test plan, "Batch 3 tests" section, in full)
- validation commands: `npx jest tests/greenfield --silent`, `npx tsc --noEmit`, `npm test`, `npm run build`
- stop conditions: do not run Gradle or require Android SDK anywhere in these tests; do not write actual scaffold files to disk; do not touch CLI/check/export files (Batch 4).
- required input artifacts: artifacts/v1.2.0-android-compose-profile-contract.txt (bundle/docs/scaffold requirements sections), artifacts/v1.2.0-android-compose-test-plan.txt (Batch 3 section), Batch 2's completed profile foundation.

BATCH 4: Android Compose CLI/check/export regression coverage

- goal: prove the full CLI surface (start/prompt/status/list/check/export) works end-to-end with an android-compose-selected greenfield run, with zero regressions to existing profiles/modes, mirroring the rigor of v1.1.0 Batch 5.
- files likely to change: primarily test files; architecture audit found no production code in check.ts/export.ts/contractChecker.ts/artifactChecker.ts that needs changing, since profile selection never reaches those layers. If Batch 2/3 uncovered a real gap requiring a production change here, this batch must report it as a deviation rather than silently expanding scope.
- tests to add/update: tests/greenfield/greenfieldCliRegression.spec.ts, greenfieldCheckExport.spec.ts (see test plan, "Batch 4 tests" section, in full)
- validation commands: `npx jest tests/greenfield --silent`, `npx tsc --noEmit`, `npm test`, `npm run build`, `node dist/cli.js --version`, `node dist/cli.js --help`, manual CLI smoke in a temp directory outside the repo (start --mode greenfield with an Android-Compose-oriented idea, through check --artifacts/check --all/export), consistent with prior batches' verification pattern.
- stop conditions: do not run Gradle; do not require Android SDK; do not add a new CLI smoke category unless Batch 4 finds scripts/cli-smoke.mjs actually needs one (the architecture audit found no evidence it does, since smoke:cli -- greenfield already exercises the CLI surface generically regardless of which profile gets selected).
- required input artifacts: artifacts/v1.2.0-android-compose-test-plan.txt (Batch 4 section), Batches 2 and 3 completed.

BATCH 5: Hardened documentation compatibility update and final implementation judge

- goal: bring every user-facing doc and the docs:check gate itself up to date with the shipped Android Compose profile, per artifacts/v1.2.0-documentation-compatibility-plan.txt in full, and produce a final judge verdict for the v1.2.0 implementation work (Batches 2-5) analogous to the judge stage in the orchestrator's own workflow model.
- files likely to change: README.md, docs/WORKFLOWS.md, docs/ARTIFACTS.md (verify only, likely no changes), docs/ARCHITECTURE.md, docs/DEVELOPMENT.md, docs/USAGE.md, CHANGELOG.md, docs/ROADMAP.md (mark v1.2.0 implemented only, no reorganization), scripts/check-docs-consistency.mjs (profile-list check refactor + misleadingAndroidClaims narrowing, per the documentation compatibility plan), src/__tests__/docs-consistency.test.ts.
- tests to add/update: src/__tests__/docs-consistency.test.ts (see test plan, "Batch 5 tests" section, in full)
- validation commands: `npm run docs:check`, `npm run lint:docs`, `npm test`, `npx tsc --noEmit`, `npm run build`, `node dist/cli.js --version`, `node dist/cli.js --help`.
- stop conditions: do not bump package.json version in this batch unless explicitly instructed (version bump is traditionally part of a release-preparation step, not a documentation batch, per the v1.1.0 precedent where the version bump happened in a docs-readiness batch commit distinct from the release-risk repair -- Batch 5 should follow whatever the user directs at that time rather than assuming); do not run pre-release/security/publish workflow (separate prompt, see below).
- required input artifacts: artifacts/v1.2.0-documentation-compatibility-plan.txt in full, plus every prior batch's implementation/test reports.

SEPARATE WORKFLOWS AFTER BATCH 5 (not part of the v1.2.0 implementation batch sequence)

- pre-release readiness: a dedicated prompt mirroring the v1.1.0 release-risk-repair prompt's structure (baseline confirmation, full local validation, evidence rules, explicit stop conditions on publish/tag/release).
- cross-platform Node 22/24 checks: run via the existing .github/workflows/validate.yml matrix (already covers both Node versions across ubuntu-latest/windows-latest/macos-15 since the v1.1.0 release-risk repair); requires pushing a branch to actually execute, which requires explicit user approval each time, per this project's own established practice.
- my-dev-kit-lab security validation: `security:validate`/`security:deps`/`security:package`/`security:semgrep`/`security:codeql` against the target repository, mirroring the v1.1.0 pre-release process exactly.
- package dry-run: `npm pack --dry-run` plus the existing `scripts/test-security.mjs` contract check.
- publish/tag/GitHub release: a separate, explicit prompt; never bundled into an implementation or documentation batch.

STATUS: complete
