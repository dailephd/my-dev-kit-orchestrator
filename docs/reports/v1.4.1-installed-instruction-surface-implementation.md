# v1.4.1 Installed Greenfield Instruction Surface Implementation

## Verdict

PASS_V1_4_1_CORRECTION_IMPLEMENTED

## Repository Baseline

- Repository: `Z:\Users\newuser\Projects\my-dev-kit-orchestrator`
- Branch: `fix/v1.4.1-installed-instruction-surface`
- Starting HEAD (this task): `f1176cc438928a3664c9d3e4d03dda832e0a1788` (`Freeze v1.4.1 instruction surface correction design`)
- Final HEAD: recorded below in Final Git Status, after the commit made by this task
- Authorization basis: `docs/reports/v1.4.1-installed-instruction-surface-audit.md` (verdict `PASS_V1_4_1_CORRECTION_DESIGN_FROZEN`), plus an explicit user decision widening the test-file scope beyond that audit's original two-file freeze (see Test-Scope Widening below).

## Correction Summary

`AFFECTED_STAGE_IDS` corrected (5 stages):

- `stage.greenfield.idea-brief`
- `stage.greenfield.starter-profile`
- `stage.greenfield.bootstrap-bundle`
- `stage.greenfield.project-docs`
- `stage.greenfield.scaffold-plan`

`FORBIDDEN_EMITTED_REFERENCES` removed from rendered output (9 references):

1. `src/greenfield/brief/loadProjectBrief.ts`
2. `normalizeProjectBrief.ts`
3. `src/greenfield/brief/briefTypes.ts`
4. `src/greenfield/profiles/resolveGreenfieldProfile.ts`
5. `src/greenfield/profiles/profileTypes.ts`
6. `src/greenfield/bootstrap/buildBootstrapBundle.ts`
7. `src/greenfield/bootstrap/bootstrapBundleTypes.ts`
8. `src/greenfield/bootstrap/validateBootstrapDocs.ts`
9. `validateGreenfieldScaffoldPlan(`

Each was replaced with directly-stated guidance (field lists, enumerated profile IDs/aliases, or a directly-stated rule) instead of a pointer to Orchestrator-internal source, per the audit's `REPLACEMENT_GUIDANCE` column. As a corollary of stating the profile-ID enumeration directly for `stage.greenfield.starter-profile`, the rendered prompt now also lists the current `SUPPORTED_PROFILES` IDs and `PROFILE_ALIASES` (computed live from `resolveGreenfieldProfile.ts`'s exports, not hardcoded), which supersedes the pre-correction requirement that this prompt must NOT enumerate profile IDs.

## Files Changed (8 total: 3 production + 5 test files, plus this report)

### Production (3)

- `src/instructions/stageInstructionContent.ts` -- removed leaks #1, #2 (idea-brief `taskInstructions`/`stopConditions`), #4 (starter-profile `taskInstructions`), #6 (bootstrap-bundle `taskInstructions`+`validationRequirements`), #8 (project-docs `stopConditions`).
- `src/promptGenerator.ts` -- removed leaks #3, #5, #7 (idea-brief/starter-profile/bootstrap-bundle "Return format: ... matching `<Type>` (`<path>`)" lines, hardcoded independently of the catalog text); added a `SUPPORTED_PROFILES`/`PROFILE_ALIASES` import and live profile-ID/alias enumeration to `greenfieldStarterProfilePrompt`.
- `src/greenfield/scaffold/renderScaffoldPrompt.ts` -- removed leak #9 (`bootstrapBundleTypes.ts` path reference and the bare `validateGreenfieldScaffoldPlan(` symbol reference) from `renderScaffoldPlanPrompt`, replacing both with a directly-stated target-path exact-match rule.

### Tests (5)

- `tests/stagePromptLeakageMatrix.test.ts` -- added a dedicated `describe` block asserting none of the 9 `FORBIDDEN_EMITTED_REFERENCES` appear in the rendered `idea-brief`, `starter-profile`, `bootstrap-bundle`, `project-docs`, or `scaffold-plan` prompts, while the required output-artifact lines, field/behavioral guidance, and a positive control for legitimate `src/...` target-project paths (`src/cli.ts`, the published `npx` index command) are preserved.
- `tests/greenfield/scaffoldPrompt.spec.ts` -- added assertions that `renderScaffoldPlanPrompt`'s output no longer contains `bootstrapBundleTypes.ts` or `validateGreenfieldScaffoldPlan(`, that the required target-path/scaffold-plan guidance is preserved, and that the specialized scaffold-plan/scaffold-implementation renderer split remains intact (not collapsed into the generic packet renderer or into each other).
- `tests/v121Batch3PromptCompatibility.test.ts` -- added a `V1_4_1_RETURN_FORMAT_CHANGED_KEYS` exclusion set (`greenfield:idea-brief`, `greenfield:starter-profile`, `greenfield:bootstrap-bundle`), following the file's own pre-existing `BATCH_4_RETURN_FORMAT_CHANGED_KEYS` convention, so the frozen `returnFormatHash` compatibility check no longer flags these three keys' now-corrected "Return format:" text as a mismatch, while every other field (header, inputs, output-artifact/file lines) and every other stage remain fully checked against the frozen `prompt-structure.json` baseline. No fixture value was mutated; the file's own established convention (already used for the Batch 4 and `final-report` exclusions) is to record known, approved return-format drift as a named exclusion rather than regenerate the frozen baseline file.
- `tests/greenfield/androidComposeCliRegression.spec.ts` -- updated the starter-profile assertion that previously required profile IDs to be absent from the rendered prompt (now asserts `android-compose` IS listed among the enumerated `Supported starter profile IDs:`, per the corrected design); updated the project-docs stop-condition assertion from the old wording (`do not claim Android/mobile support unless the selected profile is android-compose`) to the new corrected wording (`do not claim Android/mobile support in generated documentation unless the selected starter profile ID is android-compose`).
- `tests/greenfield/pythonCliProfile.spec.ts` -- updated the TST-021 assertion that previously required `stage.greenfield.starter-profile`'s `taskInstructions` to match `/canonical SUPPORTED_PROFILES registry/` (that phrase moved out of `taskInstructions` in the correction) to instead match the new `taskInstructions` wording (`supported starter profile IDs listed below`); left the `validationRequirements` assertion and the "must not contain the raw enumerated profile-ID list" assertion unchanged, since neither is affected by the correction (the enumerated list is emitted by `promptGenerator.ts`, not by the `STAGE_INSTRUCTION_CONTENT` catalog entry this test inspects).

No fixture file required editing: `tests/v121Batch3PromptCompatibility.test.ts`'s backing fixture (`tests/fixtures/v121-packet-prompts/prompt-structure.json`) was left untouched, consistent with its own file-level comment that this baseline predates later approved drift and is not regenerated.

## Test-Scope Widening (explicit user decision)

Prompt 2's original freeze (`AUTHORIZED_TEST_FILES_FOR_PROMPT_3` in the audit report) authorized exactly two test files: `tests/stagePromptLeakageMatrix.test.ts` and `tests/greenfield/scaffoldPrompt.spec.ts`. During implementation, three additional pre-existing test files were found to directly assert the pre-correction leaky wording as if it were a required, frozen contract:

- `tests/v121Batch3PromptCompatibility.test.ts` -- its frozen `returnFormatHash` fixture values for `greenfield:idea-brief`, `greenfield:starter-profile`, and `greenfield:bootstrap-bundle` were computed from the pre-correction "Return format: ... matching `<Type>` (`<path>`)" text; left unmodified, this test would fail after the correction, effectively treating the internal-source leak as protected compatibility surface.
- `tests/greenfield/androidComposeCliRegression.spec.ts` -- asserted the starter-profile prompt must NOT enumerate profile IDs, a requirement the frozen Prompt 2 design necessarily supersedes (the correction's replacement guidance for the starter-profile leak is to enumerate the profile IDs directly), and hardcoded the pre-correction exact wording of the project-docs stop condition.
- `tests/greenfield/pythonCliProfile.spec.ts` -- hardcoded the pre-correction exact phrase (`canonical SUPPORTED_PROFILES registry`) as a required substring of `taskInstructions`, which the correction necessarily removed from that field (moving the registry reference to the unaffected `validationRequirements` field and replacing `taskInstructions` with the profile-ID-enumeration-based wording).

The user was presented with this conflict and explicitly authorized widening the test-file scope to include these three files, with the explicit instruction to update them to reflect the corrected, leak-free wording without weakening what each test actually protects: `v121Batch3PromptCompatibility.test.ts` keeps its full frozen-baseline structural-compatibility mechanism for every other field and every other stage (only three keys' `returnFormatHash` are excluded, via the file's own existing exclusion-set convention); `androidComposeCliRegression.spec.ts` keeps its substantive Android/Compose profile-behavior regression coverage (mode stays `greenfield`, no Android-specific mode/blanket-forbid regressions introduced); `pythonCliProfile.spec.ts` keeps its full Python-CLI-profile registration/resolution/scaffold/validation coverage (TST-001 through TST-020 untouched; only the single stale wording assertion in TST-021 was corrected).

## Behavior Changed

- Five greenfield stage prompts (`idea-brief`, `starter-profile`, `bootstrap-bundle`, `project-docs`, `scaffold-plan`) no longer instruct the coding agent to consult `my-dev-kit-orchestrator`-internal source files or symbols that are not present when the package is installed from npm; each now states the required field set, enumerated values, or rule directly.
- `stage.greenfield.starter-profile`'s rendered prompt now additionally lists the live `SUPPORTED_PROFILES` IDs and `PROFILE_ALIASES` computed from `resolveGreenfieldProfile.ts`'s exports.
- No CLI command, workflow mode, native stage, output-artifact contract, or output file path was added, removed, or renamed. No stop-condition or validation-requirement semantic was weakened; only the internal-source-file parenthetical/pointer was removed from each corrected stop condition/task instruction.

## Negative Controls Preserved

- `stagePromptLeakageMatrix.test.ts`'s new block asserts a positive control: legitimate `src/...`-shaped target-project references (`src/cli.ts`, the published `npx @dailephd/my-dev-kit@latest index ...` command) remain present and are not caught by the forbidden-reference checks -- confirming the correction is not a blanket `src/` ban.
- `pythonCliProfile.spec.ts` TST-004/TST-006 (fuzzy/mismatched profile and stack rejections) and TST-019/TST-020 (cross-profile target-path rejection) were left untouched and still pass, confirming profile-resolution and scaffold-plan-validation negative paths are unaffected by the prompt-text correction.
- `androidComposeCliRegression.spec.ts`'s Android/mobile blanket-forbid negative assertions (`do not use Android/mobile profiles`, `do not add Android/mobile file trees`, `do not add Android/mobile behavior`, Gradle/Android-SDK/Play-Store claim checks) were left untouched and still pass.

## Retrieval / Verification Performed This Session

- `git status --short --untracked-files=all` and `git diff --name-status` at task start confirmed the working tree held exactly the 5 previously-made, uncommitted edits (`src/instructions/stageInstructionContent.ts`, `src/promptGenerator.ts`, `src/greenfield/scaffold/renderScaffoldPrompt.ts`, `tests/stagePromptLeakageMatrix.test.ts`, `tests/greenfield/scaffoldPrompt.spec.ts`), matching the prior session's claimed state.
- Read the full diff of the 5 pre-existing edits directly (`git diff`) to ground the wording used in the three newly-authorized test-file updates in the actual corrected production text, rather than the audit report's proposed wording alone.
- Read the three newly-authorized test files in full and grepped the wider `tests/` tree for every forbidden-reference/old-wording pattern, to confirm no other test file (beyond the 5 now authorized) asserts on the corrected text. Two additional files matched the grep (`tests/greenfield/androidComposeCheckExport.spec.ts`, `tests/documentationConsistency.test.ts`) but on inspection both matches were unrelated (a source-file-path mention in a comment, and an unrelated docs-consistency contract-file list); neither required changes and neither was modified.
- Inspected `src/greenfield/profiles/resolveGreenfieldProfile.ts`'s `SUPPORTED_PROFILES`/`PROFILE_ALIASES` definitions directly to confirm the exact enumerated text (`typescript-cli, nextjs-app, android-compose, python-cli`) that the corrected `starter-profile` prompt now emits, which is what made the old "must not enumerate" assertion in `androidComposeCliRegression.spec.ts` newly false.

## Tests

- Targeted run (`npx jest tests/v121Batch3PromptCompatibility.test.ts tests/greenfield/androidComposeCliRegression.spec.ts tests/greenfield/pythonCliProfile.spec.ts tests/stagePromptLeakageMatrix.test.ts tests/greenfield/scaffoldPrompt.spec.ts`): **5 suites passed, 143 tests passed, 0 failed.**
- Full suite (`npm test`): **132 of 135 suites run (3 skipped), 2934 of 2945 tests passed (11 skipped), 0 failed.** Skipped count is unchanged at 11 (matches the pre-existing baseline; no test's skip state was altered by this task).

## Validation Commands (exact sequence, in order)

| # | Command | Exit code | Result |
| --- | --- | --- | --- |
| 1 | `npm run typecheck` | 0 | PASS |
| 2 | `npm run build` | 0 | PASS |
| 3 | `npm run lint` | 0 | PASS |
| 4 | `npm run lint:docs` | 1 | **FAIL (pre-existing, not a regression -- see below)** |
| 5 | `npm run docs:check` | 0 | PASS (`DOCS_CHECK_PASS: @dailephd/my-dev-kit-orchestrator@1.4.0; published v1.4.0; 8 commands; 7 modes; 79 native stages; 13 greenfield stages; 4 greenfield profiles; 11 context-sensitive stages; schemas 1.0.0`) |
| 6 | `npm test` | 0 | PASS (2934 passed, 11 skipped, 0 failed) |
| 7 | `npm run test:security` | 0 | PASS (`SECURITY_CONTRACT_PASS: package metadata and tarball contents are safe`) |
| 8 | `npm run smoke:cli -- normal` | 0 | PASS |
| 9 | `npm run smoke:cli -- lifecycle` | 0 | PASS |
| 10 | `npm run smoke:cli -- extraction` | 0 | PASS |
| 11 | `npm run smoke:cli -- check` | 0 | PASS |
| 12 | `npm run smoke:cli -- trace` | 0 | PASS |
| 13 | `npm run smoke:cli -- correction` | 0 | PASS |
| 14 | `npm run smoke:cli -- export` | 0 | PASS |
| 15 | `npm run smoke:cli -- greenfield` | 0 | PASS |
| 16 | `npm run smoke:context` | 0 | PASS (`CONTEXT_CLI_SMOKE_PASS: source fixtures unchanged`) |
| 17 | `npm run verify` | 1 | **FAIL -- halts at the same pre-existing `lint:docs` step (typecheck/build/lint all passed before it; the script does not reach the `smoke:cli` steps because `&&`-chaining stops at the first failure)** |
| 18 | `npm pack --dry-run` | 0 | PASS (`dailephd-my-dev-kit-orchestrator-1.4.0.tgz`, 412 files, 376.8 kB packed / 1.8 MB unpacked) |

### Pre-existing `lint:docs` failure (confirmed, not a regression)

```
docs/reports/v1.4.1-installed-instruction-surface-audit.md:36 error MD040/fenced-code-language Fenced code blocks should have a language specified [Context: "```"]
docs/reports/v1.4.1-installed-instruction-surface-audit.md:73 error MD040/fenced-code-language Fenced code blocks should have a language specified [Context: "```"]
docs/reports/v1.4.1-installed-instruction-surface-audit.md:166:696 error MD033/no-inline-html Inline HTML [Element: profile-id-list]
```

All three issues are in `docs/reports/v1.4.1-installed-instruction-surface-audit.md`, a file this task is not authorized to touch (it is Prompt 2's frozen design-authorization document). This failure predates this branch's implementation work -- it was present at the starting HEAD `f1176cc438928a3664c9d3e4d03dda832e0a1788` before any of the 8 files this task changed were touched -- and is unrelated to `src/instructions/stageInstructionContent.ts`, `src/promptGenerator.ts`, `src/greenfield/scaffold/renderScaffoldPrompt.ts`, or any of the 5 test files. `npm run verify`'s failure is a direct, sole consequence of this same pre-existing condition (it halts at the identical `lint:docs` step); every check `verify` runs before that step (`typecheck`, `build`, `lint`) passed.

## Skipped Validation

None. Every command in the required Section 28 sequence was executed and its exit code recorded above; none were skipped.

## Compatibility

- All 13 native greenfield stage names, their order, and output-artifact contracts are unchanged.
- No workflow mode, CLI command, or package export was added, removed, or renamed (`docs:check` confirms 8 commands / 7 modes / 79 native stages / 13 greenfield stages / 4 greenfield profiles / 11 context-sensitive stages / schemas 1.0.0, matching the pre-correction baseline counts).
- The specialized `scaffold-plan`/`scaffold-implementation` renderer split (`src/greenfield/scaffold/renderScaffoldPrompt.ts`, separate from the generic packet-based renderer in `src/promptGenerator.ts`) is preserved; `tests/greenfield/scaffoldPrompt.spec.ts`'s new assertions explicitly confirm this.
- `tests/v121Batch3PromptCompatibility.test.ts`'s frozen `prompt-structure.json` baseline continues to gate every mode/stage's prompt filename, artifact filename(s), header, inputs block, and output-artifact/file lines; only the `returnFormatHash` field for the three corrected keys is excluded, via the file's own pre-existing exclusion-set convention (not a fixture mutation, not a weakened assertion mechanism).

## Out-of-Scope Confirmation

Not modified: `package.json`, `package-lock.json`, `README.md`, `CHANGELOG.md`, `docs/ROADMAP.md`, `docs/CURRENT_STATE.md`, `docs/documentation-preservation-manifest.json`, `docs/reports/v1.4.1-installed-instruction-surface-audit.md`, or any file outside the 8 files listed in Files Changed plus this report. No PR was opened. No merge, tag, release, or publish was performed.

## Final Git Status

See the task's closing console output (Section 42 fields) for the exact final branch, commit hashes, staged/committed file list, and `git status`/`git log` output captured immediately after the commit and push described in this task's instructions.
