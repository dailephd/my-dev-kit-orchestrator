# v1.4.1 Installed Package Acceptance

## Verdict

PASS_V1_4_1_INSTALLED_PACKAGE_ACCEPTANCE

## Repository Baseline

- Repository: `Z:\Users\newuser\Projects\my-dev-kit-orchestrator`
- GitHub repository: `dailephd/my-dev-kit-orchestrator`
- Branch: `fix/v1.4.1-installed-instruction-surface`
- Starting HEAD: `bbb47ba3dd2da7c3fd0f658659863f1b88d4d2e2`
- Working tree at start: clean
- Origin: `https://github.com/dailephd/my-dev-kit-orchestrator.git`
- Package metadata version at start: `1.4.0` (package.json, package-lock.json, and package-lock root all agree)
- Published baseline: `1.4.0` (npm `latest`, npm `@1.4.0`, local tag `v1.4.0`, and remote tag `v1.4.0` all present and consistent)

## Prior Evidence

Reviewed and accepted without rewriting:

- `docs/reports/v1.4.1-fresh-clone-preflight.md` -- baseline established (`PASS_V1_4_1_FRESH_CLONE_BASELINE_READY` equivalent: clean fresh-clone baseline confirmed, no tracked mutation).
- `docs/reports/v1.4.1-installed-instruction-surface-audit.md` -- correction design frozen across five stages (`idea-brief`, `starter-profile`, `bootstrap-bundle`, `project-docs`, `scaffold-plan`), nine forbidden references identified.
- `docs/reports/v1.4.1-installed-instruction-surface-implementation.md` -- correction implemented across 3 production files + 5 test files; the report's own recorded validation table shows `typecheck`/`build`/`lint`/`test`/all `smoke:cli` modes/`smoke:context` passing, with a documented and since-corrected `lint:docs`/`verify` formatting issue (4 markdownlint issues in two report files, unrelated to the corrected source/test files), matching this prompt's stated intent of "the implementation prompt succeeded."

Starting HEAD `bbb47ba3dd2da7c3fd0f658659863f1b88d4d2e2` is the follow-up commit that fixed those 4 markdownlint issues and corrected an inaccurate claim in the implementation report; this HEAD is CI-green on all 3 OS matrix jobs per prior established context.

## Source Candidate Validation

Executed from `Z:\Users\newuser\Projects\my-dev-kit-orchestrator` in the required order. All commands exited 0:

| # | Command | Exit | Result |
|---|---------|------|--------|
| 1 | `npm ci` | 0 | PASS (2 high severity `npm audit` advisories noted, not remediated -- out of scope) |
| 2 | `npm run typecheck` | 0 | PASS |
| 3 | `npm run build` | 0 | PASS |
| 4 | `npm run lint` | 0 | PASS |
| 5 | `npm run lint:docs` | 0 | PASS (0 issues in 22 files) |
| 6 | `npm run docs:check` | 0 | PASS |
| 7 | `npm test` | 0 | PASS |
| 8 | `npm run test:security` | 0 | PASS |
| 9-16 | `npm run smoke:cli -- <normal/lifecycle/extraction/check/trace/correction/export/greenfield>` | 0 each | PASS (all 8) |
| 17 | `npm run smoke:context` | 0 | PASS |
| 18 | `npm run verify` | 0 | PASS |
| 19 | `npm pack --dry-run` | 0 | PASS |

Jest result: `Tests: 11 skipped, 2934 passed, 2945 total`, `Test Suites: 3 skipped, 132 passed, 132 of 135 total`, 0 failed. This exactly matches the required minimum (2934 passed, exactly 11 skipped, 0 failed).

Confirmed no tracked-file mutation after validation: `git status --short --untracked-files=all`, `git diff --check`, and `git diff --name-status` were all empty.

## Candidate Tarball

- Created via `npm pack --json --pack-destination` into `Z:\Users\newuser\Projects.acceptance\my-dev-kit-orchestrator-v1.4.1\candidate-pack`.
- Exactly one tarball produced.
- Filename: `dailephd-my-dev-kit-orchestrator-1.4.0.tgz`
- Package name: `@dailephd/my-dev-kit-orchestrator`
- Package version: `1.4.0` (intentionally unchanged; this is the pre-release candidate, not the final v1.4.1 artifact)
- File count: 412
- Packed size: 376,802 bytes
- Unpacked size: 1,821,725 bytes
- SHA-256: `04df9ac4381624c4f168411efe3edf54663cadb472e47ef0053c396f70ce7cc2`

## Published v1.4.0 Comparison

- Obtained via `npm pack @dailephd/my-dev-kit-orchestrator@1.4.0 --json --pack-destination` into `published-v1.4.0-pack`.
- Exactly one baseline tarball produced: `dailephd-my-dev-kit-orchestrator-1.4.0.tgz`, 376,080 bytes packed, 412 files.
- Used only for package-surface comparison; never installed for product acceptance.

## Package Surface Comparison

- Candidate packed file count: 412. Published baseline packed file count: 412.
- Sorted path-list comparison: 0 candidate-only paths, 0 published-baseline-only paths. Exact parity.
- Package metadata surface comparison (candidate repository `package.json` vs. published tarball's `package.json`): `name`, `main` (`dist/index.js`), `bin` (`my-dev-kit-orchestrator` -> `dist/cli.js`), `engines` (`node >=24`), `files` (`["dist", "CHANGELOG.md"]`), `dependencies` (`commander` only), and `publishConfig.access` (`public`) are all identical between candidate and published baseline. No drift.

## Isolated Installation

- Acceptance root used exactly: `Z:\Users\newuser\Projects.acceptance\my-dev-kit-orchestrator-v1.4.1` (confirmed as a sibling directory to `Z:\Users\newuser\Projects` literally named `Projects.acceptance`, not a hidden folder inside `Projects`; path-safety checks on parent/leaf passed before any deletion).
- `package-host` directory: `npm init -y` then `npm install "<absolute-candidate-tarball-path>"` (local tarball only; no registry install, no `npm link`, no reuse of repository `node_modules`, no manual `dist/` copy).
- Install exit code: 0.
- Installed package path: `package-host\node_modules\@dailephd\my-dev-kit-orchestrator`
- Required files present: `package.json`, `dist\cli.js`, `dist\index.js`, `CHANGELOG.md`.
- `src`, `tests`, and `scripts` directories are absent from the installed package (confirmed not required for runtime).

## Installed Runtime Dependencies

- `npm ls --omit=dev --all` from `package-host`: exit 0, dependency tree is exactly `package-host@1.0.0 -> @dailephd/my-dev-kit-orchestrator@1.4.0 -> commander@11.1.0`.
- No development dependency (TypeScript, ts-jest, Jest, or any repository script) was installed or required to run the installed CLI. No runtime dependency leak.

## Public Command Surface

- Installed CLI path: `package-host\node_modules\.bin\my-dev-kit-orchestrator.cmd` (confirmed present via `Test-Path`).
- `--version` from `installed-cli-project` returned `1.4.0` (expected -- candidate still carries pre-release package metadata).
- `--help` and per-command `--help` (`init`, `start`, `status`, `prompt`, `list`, `mark`, `check`, `export`) all exited 0.
- Exactly these 8 public command identities are present; no command missing or extra.

## Workflow Mode Surface

- All seven modes (`feature`, `repair`, `test`, `refactor`, `harden`, `extraction`, `greenfield`) were exercised from isolated directories under `mode-smoke\`.
- For `feature`/`repair`/`test`/`refactor`/`harden`/`greenfield`: `init` then `start --mode <mode> --name "v141-<mode>-acceptance" "..."` both exited 0.
- For `extraction`: created `mode-smoke\extraction\source` and `mode-smoke\extraction\target`, ran `init` then `start --mode extraction --source <source> --target <target> --name "v141-extraction-acceptance" "..."` from the target directory; both exited 0.
- All seven modes initialize successfully from the installed package. Full workflow completion was not required or attempted.

## Cwd Independence

- Every installed-CLI command above ran with a working directory under `Z:\Users\newuser\Projects.acceptance\my-dev-kit-orchestrator-v1.4.1\...`, entirely outside `Z:\Users\newuser\Projects\my-dev-kit-orchestrator`.
- No command depended on the source repository's cwd, `node_modules`, `src`, `tests`, or `scripts`. No installed command required changing into the source repository to succeed.

## Installed Greenfield Prompt Results

- Source comparison run: `node dist/cli.js` (repository build) `init` + `start --mode greenfield --name "v141-source-prompt-acceptance" "..."` from `source-cli-project`. Exactly one run directory created: `20260905T161433-v141-source-prompt-acceptance`.
- Installed comparison run: installed CLI `init` + `start --mode greenfield --name "v141-installed-prompt-acceptance" "..."` from `installed-cli-project`. Exactly one run directory created: `20260905T161442-v141-installed-prompt-acceptance`.
- For each of the five affected stages (`idea-brief`, `starter-profile`, `bootstrap-bundle`, `project-docs`, `scaffold-plan`), the run's `prompts` directory was searched for a `.txt` file containing exactly `Stage: <stage-name>`. Exactly one match was found in the source run and exactly one match in the installed run, for all five stages (`01-idea-brief.prompt.txt`, `04-starter-profile.prompt.txt`, `05-bootstrap-bundle.prompt.txt`, `06-project-docs.prompt.txt`, `07-scaffold-plan.prompt.txt` respectively). No filename was assumed in advance; each match was located by content search.

## Forbidden Reference Results

All nine frozen forbidden references were checked (via literal-string search, not a blanket `src/` search) against all five installed affected-stage prompt files:

1. `src/greenfield/brief/loadProjectBrief.ts` -- 0 matches
2. `normalizeProjectBrief.ts` -- 0 matches
3. `src/greenfield/brief/briefTypes.ts` -- 0 matches
4. `src/greenfield/profiles/resolveGreenfieldProfile.ts` -- 0 matches
5. `src/greenfield/profiles/profileTypes.ts` -- 0 matches
6. `src/greenfield/bootstrap/buildBootstrapBundle.ts` -- 0 matches
7. `src/greenfield/bootstrap/bootstrapBundleTypes.ts` -- 0 matches
8. `src/greenfield/bootstrap/validateBootstrapDocs.ts` -- 0 matches
9. `validateGreenfieldScaffoldPlan(` -- 0 matches

Total: 0 forbidden matches across all five installed prompts.

## Preserved Guidance Results

- **idea-brief**: contains `Required output artifact: IdeaBrief`, `artifacts/idea-brief.json`, the full field-preservation list (raw idea, candidate project name, product goal, users/audience, core workflow, constraints, non-goals, preferred stack, preferred profile, platform target, documentation preferences, testing expectations, unresolved questions), and the explicit stop condition against silently defaulting to a mobile/Android profile on ambiguous product-type/platform intent. No Orchestrator source-inspection guidance present. PRESERVED.
- **starter-profile**: contains `Required output artifact: StarterProfile`, `artifacts/starter-profile.json`, all four profile IDs (`typescript-cli`, `nextjs-app`, `android-compose`, `python-cli`), all five current aliases (`python`->`python-cli`, `android`->`android-compose`, `kotlin-compose`->`android-compose`, `jetpack-compose`->`android-compose`, `compose-android`->`android-compose`), and explicit unresolved/unsupported handling. No internal source path required to explain these values. PRESERVED.
- **bootstrap-bundle**: contains `Required output artifact: GreenfieldBootstrapBundleArtifact`, `artifacts/bootstrap-bundle.json`, and direct guidance for all current top-level bundle responsibilities (normalized brief, selected profile, starter profile summary, stack decision, template target categories, doc-generation instructions, scaffold-planning inputs, validation rules, unresolved decisions, full-stack capability selection). No pointer to `buildBootstrapBundle.ts` or `bootstrapBundleTypes.ts`. PRESERVED.
- **project-docs**: contains `Required output artifact: ProjectDocsReport`, `artifacts/project-docs-report.txt`, the explicit restriction against claiming Android/mobile support unless the selected profile ID is `android-compose`, and the existing release/security/publish-completion stop condition. No pointer to `validateBootstrapDocs.ts`. PRESERVED.
- **scaffold-plan**: contains `Required output artifact: ScaffoldPlan`, `artifacts/scaffold-plan.txt`, `scaffoldPlanningInputs` guidance, direct target-path guidance (required-target and path-safety meaning), an explicit prohibition on inventing additional target paths, and the existing scaffold-plan stop conditions. `bootstrapBundleTypes.ts` and `validateGreenfieldScaffoldPlan(` are both absent. The specialized plain-text renderer template (distinct from the generic instruction-packet format) remains intact and was not replaced with generic packet text. PRESERVED.

## Legitimate Target Path Negative Controls

- Installed `scaffold-implementation` prompt (`08-scaffold-implementation.prompt.txt`) still contains the legitimate target-project example path `src/cli.ts`.
- Installed `initial-index` prompt (`11-initial-index.prompt.txt`) still contains the published-package invocation `npx @dailephd/my-dev-kit@latest index --root . --src src --out .my-dev-kit --json`, preserving `--src src`.
- Result: PASS. The correction removed inappropriate Orchestrator-maintainer source-path leakage without becoming a blanket ban on `src/`-shaped guidance for the generated target project.

## Source Versus Installed Prompt Parity

For each of the five affected stages, the source-build and installed-package prompt text were normalized (CRLF stripped; the two runs' distinct run-ID strings and project-directory-name strings, in both forward- and back-slash contexts, mapped to common placeholders) and compared. No instruction text, artifact name, field name, profile ID, alias, stop condition, validation guidance, stage name, or source-reference string was altered by normalization.

| Stage | Normalized `diff` | SHA-256 (source) | SHA-256 (installed) | Match |
|---|---|---|---|---|
| idea-brief | empty (identical) | `a87fdb52706558aaadd6634d5dcdec64bf0b4afe8b1d9114d120861d836eb485` | same | YES |
| starter-profile | empty (identical) | `c55835806ac5fa3524955828c399e199bf071bdc0afae812b8c16cdf4999a79d` | same | YES |
| bootstrap-bundle | empty (identical) | `55fd41a4d123fa8c7da8072469b1097900165c0435c54d703c98c268eeab7915` | same | YES |
| project-docs | empty (identical) | `1bd9920bebac9269556b8daa3f0a392e060754206f7e3b7fe1973ad3b4e25bf1` | same | YES |
| scaffold-plan | empty (identical) | `77a4a69d322dbec4668d9fc6821f5cdea239f56f50eb346e57ac1e309f2515e9` | same | YES |

All five stages: exact normalized parity confirmed.

## Documentation Reconciliation

Modified exactly three temporal/state documents plus this report:

- `docs/ROADMAP.md` -- updated the version summary, roadmap-sequence statement, and the detailed `v1.4.1` milestone section from future tense ("will correct") to completed-but-unreleased state ("Status: Implementation and packed-install validation complete; not released."); corrected the historical four-stage planning assumption to the verified five-stage result (added `project-docs`); recorded at high level that all 13 greenfield stages were audited, five required correction, nine internal references were removed, the correction spans the generic instruction path/`promptGenerator` return-format path/specialized scaffold-plan renderer, and installed tarball behavior matches source behavior for the corrected prompts. No per-batch chronology, command transcript, test counts, commit hashes, or prompt/session names were added.
- `docs/CURRENT_STATE.md` -- publication identity kept unchanged (package metadata version `1.4.0`, current release `v1.4.0`). Updated the current-limitations section to state explicitly that the published `v1.4.0` package still ships the five-stage prompt-content defect, while the maintained `fix/v1.4.1-installed-instruction-surface` branch now contains a validated, packed-install-accepted correction not yet available to npm users. Updated "Active next direction" to the required sequence (release `v1.4.1`; then `v1.5.0`; then `v1.6.0`; Greenfield-to-Feature handoff deferred; mobile profiles deferred).
- `docs/documentation-preservation-manifest.json` -- kept `packageMetadataVersion: "1.4.0"` and `latestPublishedVersion: "1.4.0"` unchanged; set `implementedUnpublishedVersions: ["v1.4.1"]` and `plannedUnpublishedVersions: ["v1.5.0", "v1.6.0"]`; `roadmapVersions` and all implementation-derived facts (8 commands, 7 modes, 79 native stages, 13 greenfield stages, 4 profiles, schema versions, etc.) left unchanged.
- `docs/reports/v1.4.1-installed-package-acceptance.md` -- this report (new file).

No other documentation file (README.md, CHANGELOG.md, docs/RELEASE.md, docs/RELEASE_CHECKLIST.md) was modified.

## Compatibility Invariants

- `NO_CORE_RETRIEVAL_ENGINE_CHANGE_REQUIRED`: preserved.
- All 13 native greenfield stage names, order, and output-artifact contracts unchanged.
- No workflow mode, CLI command, or package export added, removed, or renamed.
- The specialized `scaffold-plan`/`scaffold-implementation` renderer split is preserved.
- No dependency added; `commander` remains the only runtime dependency.
- Package file surface (412 files) and package metadata surface (`main`, `bin`, `engines`, `files`, `dependencies`, `publishConfig.access`) are unchanged from the published `1.4.0` baseline.

## Remaining Risks

- `npm audit` continues to report 2 high-severity advisories in the dependency tree (unchanged from prior reports); not investigated or remediated here, as this task is scoped to packed-install acceptance and documentation reconciliation, not dependency remediation.
- The acceptance directory `Z:\Users\newuser\Projects.acceptance\my-dev-kit-orchestrator-v1.4.1` remains on disk outside the repository; it was not committed and contains no tracked repository content.
- Full workflow completion (beyond `init`/`start` initialization) was not exercised for the six non-extraction workflow modes or for extraction; this is consistent with the runbook's stated scope ("this prompt does not require completing these workflows").

## Release Readiness

READY_FOR_V1_4_1_RELEASE_PREPARATION

## Next Step

Run the standardized v1.4.1 release-preparation and publication workflow. That workflow must bump package metadata to 1.4.1, finalize release-state documentation and CHANGELOG, create and validate the exact release candidate, merge through GitHub Actions, tag v1.4.1, create the GitHub Release with title v1.4.1, and run `npm publish --access public` only as the final state-changing publication command.
